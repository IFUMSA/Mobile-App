# Backend API URL (for API proxy routes - server-side only)
BACKEND_URL=http://localhost:5000

# Optional: Public API URL (for direct client-side calls if needed)
NEXT_PUBLIC_API_URL=http://localhost:5000
