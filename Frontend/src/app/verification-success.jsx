import VerificationSuccess from '@components/features/auth/verify-mail/success';
import Container from '@components/ui/container';

const VerificationSuccessScreen = () => {
  return (
    <Container>
      <VerificationSuccess />
    </Container>
  );
};

export default VerificationSuccessScreen;
